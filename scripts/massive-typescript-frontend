#!/usr/bin/env bash
set -euo pipefail
repository="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
exec deno run --config "$repository/deno.json" --allow-read --allow-env --allow-sys=cpus "$repository/packages/sdk/src/frontend.ts" "$@"
